import type { AppSettings } from '../types';

export const DEFAULT_SETTINGS: AppSettings = {
  activeProviderId: 'mock',
  providers: {},
  theme: 'system',
};

export const DEFAULT_CATEGORIES = ['写作', '编程', '营销', '翻译', '分析', '创意', '学习', '其他'];
